services:
  engine:
    build: .
    env_file: .env
    environment:
      - DB_PATH=/data/autogolpe.db
      - NODE_OPTIONS=--max-old-space-size=256
    volumes:
      - ./data:/data
    restart: unless-stopped
